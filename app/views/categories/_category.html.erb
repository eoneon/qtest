<tr>
    <td><%= category.id %></td>
    <td><%= category.kind %></td>
    <td><%= category.name %></td>
    <td><%= link_to 'Show', [category] %> | <%= link_to 'Edit', [:edit, category] %> | <%= link_to 'Delete', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
