<%= form_for @item, :html => {:id => 'item_form'} do |f| %>
<p>D: <%= @item.build_descrp("tag")[45..56] %></p>
<p>first_word_idx_rng: <%= @item.first_word_idx_rng(@item.test_str) %></p>
<p>first_chars_idx_rng: <%= @item.first_chars_idx_rng(@item.test_str) %></p>
<p>next_chars_idx: <%= @item.next_chars_idx(@item.test_str, 8) %></p>
<p>TEST HERE!: <%= @item.xl_dim_idx(@item.build_descrp("tag")) %></p>
<p>idx: <%= @item.test_str[6] %></p>
<p>regexp: <%= @item.test_str.index(/[^\s]/, 6) %></p>
<p>next_chars_idx_rng: <%= @item.next_chars_idx_rng(@item.test_str, 6) %></p>
<p>xl_dim_rng: <%= @item.xl_dim_idxs(@item.build_descrp("tag")) %></p>
  <%= render "items/hidden", f: f %>
  <script type="text/javascript">
    function submitform(){
      $('#redirect_location').val(':edit');
      $('#item_form').submit();
    }
  </script>
  <script type="text/javascript">
    function reload(){
      var item_type_id = $('#selectItemtype').val();
      $('#item_item_type_id').val('item_type_id');
    }
  </script>

  <div class="row">
    <div class="col-md-2">
      <%= f.label :sku %><br>
      <%= f.text_field :sku %>
    </div>
  </div>

  <% type_list.each do |type| %>
    <%= render "items/type", type: type, f: f %>
  <% end %>

  <%= render "items/properties", parent: @item, f: f %>
  <%= f.submit "Save" %>
  </div>
<% end %>
