<%= form_for @item, :html => {:id => 'item_form'} do |f| %>
<p><strong>valid_keys:</strong> <%= @item.ordered_keys("inv") %></p>
<p><strong>valid_tag_mount?:</strong> <%= @item.insert_at_idx(@item.ordered_keys("inv"), "dog", "sign", -1) %></p>
<p><strong>tag_list:</strong> <%#= @item.insert_item(@item.ordered_keys("inv"), "dog", ) %></p>
<p><strong>valid_existing_types:</strong> <%#= @item.valid_existing_types %></p>
<p><strong>build_d:</strong> <%#= @item.build_d("tag") %></p>
  <%= render "items/hidden", f: f %>
  <script type="text/javascript">
    function submitform(){
      $('#redirect_location').val(':edit');
      $('#item_form').submit();
    }
  </script>
  <script type="text/javascript">
    function reload(){
      var item_type_id = $('#selectItemtype').val();
      $('#item_item_type_id').val('item_type_id');
    }
  </script>

  <div class="row">
    <div class="col-md-2">
      <%= f.label :sku %><br>
      <%= f.text_field :sku %>
    </div>
  </div>

  <% type_list.each do |type| %>
    <%= render "items/type", type: type, f: f %>
  <% end %>

  <%= render "items/properties", parent: @item, f: f %>
  <%= f.submit "Save" %>
  </div>
<% end %>
